name: atmakitchen_mobile
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Common Depedencies
  intl: ^0.18.0
  http: ^1.1.0
  http_parser: ^4.0.2

  # UI
  tailwind_colors: ^0.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
